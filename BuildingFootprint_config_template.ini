[PATHS]
scrape_log_path = Path to scrape log
distribute_log_path = Path to distribution log
zip_dir_path = Path to local temporary directory
lyr_dir_path = Path to Buildings and Lots layer directory
sde_path = Path to Production SDE
template_path = Path to metadata template xml
missing_bbls_path = Path to missing bbl csv

[PROXIES]
http_proxy = HTTP proxy credentials
https_proxy = HTTPS proxy credentials

[URLS]
bldg_url = Direct shapefile download URL
update_url = URL for page containing shapefile update date information